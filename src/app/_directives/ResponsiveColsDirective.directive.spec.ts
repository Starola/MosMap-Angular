/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ResponsiveColsDirectiveDirective } from './ResponsiveColsDirective.directive';

describe('Directive: ResponsiveColsDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveColsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
